import React from "react";
import ReactDOM from "react-dom/client";

function App() {
    return <h1>Welcome to useContext_vita325</h1>;
}

// Рендеринг компоненту App у <div id="root">
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
